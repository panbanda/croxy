pre-push:
  parallel: false
  commands:
    fmt:
      run: cargo fmt --all -- --check
    clippy:
      run: cargo clippy --all-targets --all-features -- -D warnings
    omen:
      run: omen score --check
